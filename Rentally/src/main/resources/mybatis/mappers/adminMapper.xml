<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rental.shinhan.admin">

    <select id="selectProducts" resultType="com.rental.shinhan.dto.ProductDTO">
        select * from product;
    </select>

    <select id="selectReviews" resultType="com.rental.shinhan.dto.ReviewDTO">
        select *
        from review
        order by review_date desc;
    </select>

    <select id="selectOrders" resultType="com.rental.shinhan.dto.OrderJoinDTO">
        select
        p.product_img,
        p.product_serial,
        c.cust_name,
        s.sub_date,
        s.sub_card,
        s.sub_total,
        s.sub_period
        from
        subscribe s
        join
        customer c on s.cust_seq = c.cust_seq
        join
        product p on s.product_seq = p.product_seq;
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rental.shinhan.cart">

	<select id="selectCart" parameterType="String"
		resultType="com.rental.shinhan.dto.CartJoinDTO">
		select product_brand, product_name, product_img, product_pay, cart_option
		from cart
		join product
		on cart.product_seq = product.product_seq
		join customer
		on customer.cust_seq = cart.cust_seq
		where customer.cust_id = #{cust_id};
	</select>
	
</mapper>
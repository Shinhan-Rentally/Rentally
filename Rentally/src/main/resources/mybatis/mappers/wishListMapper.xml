<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rental.shinhan">

    <select id="selectWishLists" parameterType="int" resultType="com.rental.shinhan.dto.WishListJoinDTO">
        select w.wish_seq, w.cust_seq, w.product_seq,
        p.product_serial, p.product_name, p.product_img, p.product_features
        from wishlist w
        join product p
        on w.product_seq = p.product_seq
        where w.cust_seq = #{cust_seq};
    </select>

</mapper>